---
galaxy_info:
  author: Marc Zottner
  description: Andvanced role for Red Hat JBoss Web Server (httpd)
  license: license (Apache)
  min_ansible_version: 2.1.1.0
  platforms:
  - name: EL
    versions:
    - 7
  categories:
  - cloud
  - development
  - packaging
  - system
  - java
  - jboss
  - common

# Several instances cannot be setup on the same host (cf. https://docs.ansible.com/ansible/playbooks_roles.html#task-include-files-and-encouraging-reuse)
allow_duplicates: no

dependencies:
    - { role: rh-jboss-common }
